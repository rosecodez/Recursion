// Build a function which sorts a given array but uses a “merge sort” function

const array = [5, 1, 7, 3, 2, 8, 6, 4];

function mergeSort() {
  const arraySize = array.length;
  const leftArray = array.slice(0, 4);
  const rightArray = array.slice(4, 8);
}
mergeSort();
